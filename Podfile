# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'localizr' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for localizr
  pod 'LibTorch', '~>2.1.0'

  target 'localizrTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'localizrUITests' do
    # Pods for testing
  end

end
